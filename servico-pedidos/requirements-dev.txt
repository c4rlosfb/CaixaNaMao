# =============================================================================
# Dependências de DESENVOLVIMENTO/TESTE do servico-pedidos.
# Não entram na imagem de runtime (ver Dockerfile: estágio `test`).
#   pip install -r requirements-dev.txt
# =============================================================================
-r requirements.txt

# --- Testes ---
pytest==8.3.3
pytest-asyncio==0.24.0
pytest-cov==5.0.0
aiosqlite==0.20.0        # SQLite async para testes (sem infra PostgreSQL)
httpx==0.27.2            # AsyncClient usado nos testes

# --- Qualidade de código e geração de stubs (não vão para a imagem) ---
ruff==0.16.8
grpcio-tools==1.66.1
