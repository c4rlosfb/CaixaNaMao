# --- Framework REST ---
fastapi==0.115.0
uvicorn[standard]==0.30.6

# --- Banco de dados (async) ---
sqlalchemy==2.0.35
alembic==1.13.3
asyncpg==0.29.0          # driver async para PostgreSQL (substitui psycopg2-binary no runtime)
psycopg2-binary==2.9.9   # mantido para Alembic CLI (sync) e scripts de migration

# --- gRPC (cliente para chamar servico-estoque) ---
grpcio==1.66.1
grpcio-tools==1.66.1

# --- Mensageria assíncrona (AWS SQS) ---
boto3==1.35.18

# --- Autenticação JWT (validação local HS256) ---
pyjwt==2.9.0

# --- Utilitários ---
python-dotenv==1.0.1
pydantic-settings==2.5.2
httpx==0.27.2            # AsyncClient usado nos testes

# --- Testes ---
pytest==8.3.3
pytest-asyncio==0.24.0
pytest-cov==5.0.0
aiosqlite==0.20.0        # SQLite async para testes (sem infra PostgreSQL)
