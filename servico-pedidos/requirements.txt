# =============================================================================
# Dependências de RUNTIME do servico-pedidos (imagem final).
# Dependências de desenvolvimento/teste ficam em requirements-dev.txt.
# =============================================================================

# --- Framework REST ---
fastapi==0.115.0
uvicorn[standard]==0.30.6

# --- Banco de dados (async) ---
sqlalchemy==2.0.35
alembic==1.13.3
asyncpg==0.29.0          # driver usado pelo runtime E pelo Alembic (env.py usa engine async)

# --- gRPC (cliente do servico-estoque) ---
grpcio==1.66.1
# Runtime do protobuf exigido pelos stubs gerados por grpcio-tools.
protobuf==5.29.6

# --- Mensageria assíncrona (AWS SQS) ---
boto3==1.35.18

# --- Autenticação JWT (validação local HS256) ---
pyjwt==2.9.0

# --- Utilitários ---
python-dotenv==1.0.1
pydantic-settings==2.5.2
