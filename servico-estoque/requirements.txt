# --- Servidor gRPC ---
grpcio==1.66.1
grpcio-tools==1.66.1

# --- Health check HTTP (FastAPI) ---
fastapi==0.115.0
uvicorn[standard]==0.30.6

# --- Banco de dados ---
sqlalchemy==2.0.35
alembic==1.13.3
asyncpg==0.29.0          # driver async usado pelo runtime
psycopg2-binary==2.9.9   # usado apenas pelo Alembic CLI (sync)

# --- Distributed lock (Redis) ---
redis==5.1.0

# --- Mensageria assíncrona (AWS SQS) ---
boto3==1.35.18

# --- Utilitários ---
python-dotenv==1.0.1
pydantic-settings==2.5.2

# --- Testes ---
pytest==8.3.3
pytest-asyncio==0.24.0
pytest-cov==5.0.0
aiosqlite==0.20.0        # SQLite async para testes de domínio (sem infra)
fakeredis[lua]==2.24.1   # Redis em memória com EVAL (testa o script Lua do ADR-004)
httpx==0.27.2            # AsyncClient dos testes de health check
