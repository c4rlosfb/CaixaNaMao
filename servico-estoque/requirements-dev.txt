# =============================================================================
# Dependências de DESENVOLVIMENTO/TESTE do servico-estoque.
# Não entram na imagem de runtime (ver Dockerfile: estágio `test`).
#   pip install -r requirements-dev.txt
# =============================================================================
-r requirements.txt

# --- Testes ---
pytest==8.3.3
pytest-asyncio==0.24.0
pytest-cov==5.0.0
aiosqlite==0.20.0        # SQLite async para testes de domínio (sem infra)
fakeredis[lua]==2.24.1   # Redis em memória com EVAL (testa o script Lua do ADR-004)
httpx==0.27.2            # AsyncClient dos testes de health check

# --- Qualidade de código e geração de stubs (não vão para a imagem) ---
ruff==0.16.8
grpcio-tools==1.66.1     # usado por scripts/gerar_stubs.sh
